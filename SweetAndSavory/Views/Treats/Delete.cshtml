@{
  Layout = "_Layout";
}

@model SweetAndSavory.Models.Treat

<h2>Are you sure you want to delete this treat?</h2>

@Html.DisplayNameFor(model => model.TreatName): @Html.DisplayFor(model => model.TreatName)
@using (Html.BeginForm())
{
  <input type="submit" value="Delete" />
}
<div class="hstack gap-3 pt-3 mt-3 border-top border-dark">
  @Html.ActionLink("Back to List", "Index", new {}, new {@class="btn btn-light"})
</div>